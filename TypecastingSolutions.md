# CSCI 240 - Supplemental Instruction

### Typecasting Answer Key

---

```c++
C = ((int) A) % ((int) B);
D = ((int) A) / ((int) B);
```

---

```c++
B = (int) A;
C = A - B;
```

---

```c++
C = (int) (A * B);
D = (int) (A + B);
```